# Lisans anahtarları (gizli)

`F2F-LICENSE-KEYS-PRIVATE.csv` bu klasörde üretilir ve **gitignore**’dadır.

Yeniden üretmek:

```bash
node tools/generate-licenses.mjs
```

Satışta CSV’de `status` / `sold_to` / `sold_at` doldurun. Bu dosyayı plugin ZIP’ine koymayın.
